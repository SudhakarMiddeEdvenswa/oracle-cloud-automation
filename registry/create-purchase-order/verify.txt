# Create Purchase Order — verification rules
#
# One rule per line, run after the flow's steps. {{placeholders}} resolve
# against the row data plus everything the run captured.
#
#   open-tab: / text-visible: / text-not-visible:
#   field-equals: <label> = <value>      field-contains: <label> = <value>
#   captured-not-empty: <name>           captured-matches: <name> = <regex>
#   ai: <claim judged by the AI agent>

captured-not-empty: orderNumber
captured-not-empty: orderStatus
text-visible:       {{orderNumber}}
ai:                 Purchase order {{orderNumber}} exists for supplier {{supplier}} with description {{description}}, has one line for {{itemDescription}} with quantity {{quantity}} at price {{price}}, and a status consistent with a successful submission. No error banner is present.
