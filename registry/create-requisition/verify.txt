# Create Pur Requisition — verification rules
#
# One rule per line, run after the flow's steps. {{placeholders}} resolve
# against the row data plus everything the run captured.
#
#   open-tab: / text-visible: / text-not-visible:
#   field-equals: <label> = <value>      field-contains: <label> = <value>
#   captured-not-empty: <name>           captured-matches: <name> = <regex>
#   ai: <claim judged by the AI agent>

captured-not-empty: requisitionNumber
captured-not-empty: requisitionStatus
text-visible:       {{requisitionNumber}}
ai:                 Requisition {{requisitionNumber}} exists with description {{requisitionDescription}}, one line for {{itemDescription}} with quantity {{quantity}}, and a status consistent with a successful submission (for example Pending approval, In process or Approved). No error banner is present.
